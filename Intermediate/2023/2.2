a = int(input("Area of base: "))
b = int(input("Step: "))
start = 10
stop = 21
for n in range(start, stop, b):
  v = a * n
  print(f'Height: {n} cm, Volume: {v} cm³')
----------------------------------------------
slang = ['GTG', 'LOL', 'IMD', 'IDK', 'IDC', 'CTC?', 'BTW', 'TBH', 'LMK', 'NVM']
word_list = input('Text message: ').split()
zed_words = 0
for word in range((word_list)):
  if word_list[word] in slang:
    zed_words = zed_words + 1

print(f'Number of slang words used: {zed_words}')
----------------------------------------------------------------
dances = ['ballet', 'tap', 'jazz', 'belly', 'contemporary', 'ballroom', 'salsa', 'step', 'swing', 'irish', 'modern', 'shuffle', 'floss', 'twisting', 'macarena', 'interpretive', 'wobble', 'flick', 'stomp', 'tango']

def viral_check(name):
  #Find how viral this dance will go
  level = ''
  return

count = 0
#Write the rest of your program after this.
a = input("Enter names: ").split()
for i in range(len(a)):
  if a[i] in dances:
    print(f"The '{a[i]}' will go nowhere, the dance already exists!")
  elif len(a[i]) == 8:
    print(f"The '{a[i]}' will go not viral!")
  elif len(a[i]) < 4:
    print(f"The '{a[i]}' will go slightly viral!")
  elif 'j' in a[i]:
    print(f"The '{a[i]}' will go somewhat viral!")
  else:
    count = count + 1
print(f"Ultra Viral names: {count}")
  
    
