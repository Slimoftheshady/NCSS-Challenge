def tall_enough(height):
  # Write your function here.
  return

# Write the rest of your program here.
a = int(input("How tall are you? "))
if a >= 130:
  print("You can ride the ship of the desert!")
elif a >= 100:
  print("You can ride the pony!")
else:
  print("You can ride the teacups!")
--------------------------------------------------
def sale_price(original, discount):
  #Calculate the sale price here.
  return

#Write the rest of your program here
#Grab the input (create the user prompts!)
p = input("Product: ")
op = float(input("Original price ($): "))
d = float(input("Discount (%): "))

sale = d / 100
john = 1 - sale
ee = john * op
eo = round(ee, 2)
print(f"{p} on sale for ${eo}.")
-----------------------------------------------
def calculate_iron(ore):
  # Calculate the Iron from Ore, to 1 decimal place
  return

# Write the rest of your program here
a = float(input("Ore: "))
c = a * 5/13
b = round(c, 1)
if b >= 45:
  print(f"{b} Iron - Magic amulet")
elif b >= 35:
  print(f"{b} Iron - Fancy shield")
elif b >= 20:
  print(f"{b} Iron - Sword")
elif b >= 5:
  print(f"{b} Iron - Pickaxe")
  
else:
  print(f"{b} Iron - Useless lump")




